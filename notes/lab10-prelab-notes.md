## Exercise 1

*write a customer class* resulted in a decent enough file but in the wrong directory, though the customer ID was an int instead of a string like we would prefer

*plain Java 21 record/class for Northstar CRM customer CUS-1001 Amina Khan status ACTIVE; fields id, fullName, status; no Spring, no JPA; correlation note lab-request-001 in comments only.* Resulted in a more specific prompt where technically the agent completed the task, however the ai decided to use an enumerator for if a customer was active when it should have used a boolean for performance reasons.

## Exercise 5

java -version
javac -version
git --version
mvn -version

Path should be set to JDK 21 and Maven 3.9.6+ for this lab, java 8 is available on this machine.

## Exercise 6

[ ] Weak vs strong prompts documented\
[ ] Phantom annotations flagged, reject rule written\
[ ] Customer sketch: CUS-1001 Amina ACTIVE, CUS-1002 Ravi PROSPECT\
[ ] Review-log TODOs filled; java/mvn confirmed on JDK 21

